VAR
	[FLOAT, INT]:[c1, e1]
	[STRING, STRING]:[z]
	[FLOAT, INT, STRING, INT, INT, INT, INT, INT]:[a1, b1, var1, a, b, c, d, r]
ENDVAR

--/soy un comentario/--

b = 123
c = 666
a = b
var1 = "asd"
a1 = 1.99
b1 = 99.2

READ d
PRINT "5"

IF (NOT((3) > 4))
	c = 666
ENDIF

REPEAT 
    b = a
UNTIL (a == 2.3 OR 1==1)

IF ( a < (a*3) )
    IF (a == b AND a > c)
        a = a + b
    ENDIF
ENDIF
